path: flexible_manipulation_flexbe_states.get_planning_scene_state
class: GetPlanningSceneState

import_only: false

params:
  timeout       : 1.0
  wait_duration : 0.5
  action_topic  : None

input:
  action_topic : "/get_planning_scene"
  components : None

outcome:
    # We expect this to fail if running stand alone
    # without having anything to connect to
    failed
