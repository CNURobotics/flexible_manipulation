path: flexible_manipulation_flexbe_states.get_joint_names_from_move_group_state
class: GetJointNamesFromMoveGroupState

import_only: false

input:
    selected_move_group : arm
    robot_name : kinova

outcome:
    # We expect this to fail if running stand alone
    # without having anything to connect to
    param_error  # If we first setup the proxy
